config:
  name: istio
  namespace: istio-system
  global:
    k8sIngress:
      enabled: true # Create the auto-generated ingress

subcomponents:
  istio-crd:
    config:
      namespace: istio-system
