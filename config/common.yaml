config:
  name: istio
  namespace: istio-system
  global:
    k8sIngress:
      enabled: true # Create the auto-generated ingress
    proxy:
      includeIPRanges: "0.0.0.0" # Allow all egress by default

subcomponents:
  istio-crd:
    config:
      namespace: istio-system
