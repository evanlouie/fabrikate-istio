config:
  name: istio
  namespace: istio-system
  global:
    proxy:
      includeIPRanges: "0.0.0.0" # Allow all egress by default
    k8sIngressSelector: ingressgateway # Change selector to match the istio-autogenerated-k8s-ingress which gets auto provisioned
subcomponents:
